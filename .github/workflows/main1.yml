on:
  push:
    branches: [ "main" ]
    paths: [ "src/**" ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "main" ]
    paths: [ "src/**" ]
  schedule:
    - cron: '34 5 * * 1'
